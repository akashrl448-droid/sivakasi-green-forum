PORT=4000
NODE_ENV=production
FRONTEND_ORIGIN=http://localhost:5500

# Security: Set a strong random secret (32+ chars)
JWT_SECRET=your-strong-production-jwt-secret-here

# Optional: AWS S3 & CloudFront (Leave blank to use local server storage)
AWS_REGION=ap-south-1
AWS_BUCKET_NAME=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
CLOUDFRONT_URL=
